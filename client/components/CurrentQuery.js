import React, { Component } from 'react';
import PropTypes from 'prop-types';

const CurrentQuery = ({ cuurentQuery }) => (
    <div className="search-container">

    </div>     
);

Search.propTypes = {
    currentQuery: PropTypes.string.isRequired
}

export default CurrentQuery;